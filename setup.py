from setuptools import setup, find_packages

setup(name='deep_sort_realtime', 
        version='1.0', 
        packages=['deep_sort_realtime'],
        install_requires=[
            'numpy',
            'scipy',
        ]
    )
